[[questions]]
id = "d34c3b07-12df-4855-b47c-5bb045757613"
type = "Tracing"
prompt.program = """
void main() {
    String greeting = "привет";
    greeting.toUpperCase();
    IO.println(greeting);
}
"""
answer.doesCompile = true
answer.stdout = "привет"
context = """
Строки неизменяемы. `toUpperCase()` не меняет строку, а возвращает новую — и
эта новая здесь никуда не записана и тут же выброшена.

Компилятор о такой бесполезной строке не предупреждает: вызвать метод и
проигнорировать результат — законное действие.
"""

[[questions]]
id = "ca3e0aa9-a55b-4421-bb78-fcaeb51783d0"
type = "MultipleChoice"
prompt.prompt = """
`Integer a = 127, b = 127;` — сравнение `a == b` даёт `true`. А для
`Integer c = 128, d = 128;` сравнение `c == d` даёт `false`. Почему?
"""
answer.answer = "`==` сравнивает ссылки, а значения от −128 до 127 виртуальная машина кеширует и переиспользует."
prompt.distractors = [
  "128 не помещается в `byte`, поэтому создаётся другой тип.",
  "Это ошибка конкретной версии JVM.",
  "`Integer` сравнивается по значению только до 127 по спецификации языка."
]
context = """
Запоминать границу кеша не нужно — нужно не сравнивать объекты через `==`.
Для `Integer` правильный способ — `c.equals(d)`, и он даёт `true` для любых
значений.

Правило: примитивы сравнивают через `==`, объекты — через `equals`.
"""
